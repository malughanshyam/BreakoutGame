package gameInit;

/**
 * Constants used by the game
 * 
 */

public interface Constants {
	int WIN_WIDTH = 600;
	int WIN_HEIGHT = 700;
	int BRICK_ROWS = 4;
	int BRICK_COLUMNS = 5;
	int TOTAL_BRICKS = BRICK_ROWS * BRICK_COLUMNS;
	int LEFTMARGIN = WIN_WIDTH / 8;
	int TOPMARGIN = WIN_HEIGHT / 8;
	int TOPOFFSET = WIN_HEIGHT / 50;
	int LEFTOFFSET = WIN_WIDTH / 5;

}
